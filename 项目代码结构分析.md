# Kode-Ocean 项目代码结构分析

> 生成时间: 2025-10-25
> 分析范围: src/ 目录全部代码
> 项目类型: TypeScript + React (Ink CLI框架)

## 📋 目录
1. [项目概览](#1-项目概览)
2. [核心架构层](#2-核心架构层)
3. [配置与模型管理](#3-配置与模型管理)
4. [AI服务集成层](#4-ai服务集成层)
5. [工具系统](#5-工具系统)
6. [命令系统](#6-命令系统)
7. [UI组件系统](#7-ui组件系统)
8. [Hooks系统](#8-hooks系统)
9. [工具类和基础设施](#9-工具类和基础设施)
10. [类型和常量](#10-类型和常量)
11. [模块依赖关系图](#11-模块依赖关系图)

---

## 1. 项目概览

### 1.1 项目定位
Kode-Ocean 是一个基于Claude AI的命令行界面（CLI）工具，提供：
- **多模型支持**: Claude、OpenAI、Deepseek、Gemini等多种AI模型
- **交互式对话**: 基于Ink框架的丰富终端UI
- **工具系统**: 文件操作、代码搜索、Bash执行等多种工具
- **Agent系统**: 支持动态加载和热重载的Agent配置
- **MCP协议**: 支持Model Context Protocol扩展

### 1.2 技术栈
- **核心框架**: React + Ink (CLI UI框架)
- **语言**: TypeScript (strict mode)
- **构建工具**: Bun
- **AI SDK**: @anthropic-ai/sdk, openai等
- **CLI框架**: commander.js
- **验证**: Zod

### 1.3 代码统计
```
总代码量: ~50,000行
核心模块分布:
- 入口与UI: ~4,000行 (cli.tsx 1,506行 + REPL.tsx 809行)
- 服务层: ~6,000行 (claude.ts 2,165行 + openai.ts 1,361行等)
- 工具系统: ~5,000行 (20+个工具实现)
- UI组件: ~8,000行 (ModelSelector.tsx 3,486行等)
- 命令系统: ~5,000行 (agents.tsx 3,416行等)
- 工具类: ~10,000行 (debugLogger 1,232行, config 939行等)
- Hooks: ~3,000行 (useUnifiedCompletion 1,404行等)
```

---

## 2. 核心架构层

### 2.1 入口点模块 (`src/entrypoints/`)

#### **src/entrypoints/cli.tsx** (1,506行)
**主要功能**:
- CLI应用程序主入口
- 命令行参数解析和路由
- 配置系统初始化
- 启动向导和对话管理

**核心职责**:
1. **命令行解析**: 使用`commander.js`解析命令和选项
2. **配置管理**:
   - 初始化全局配置和项目配置
   - GPT-5模型配置自动修复
   - MCP服务器配置管理
3. **模式选择**:
   - 交互模式 (默认): 启动REPL界面
   - 打印模式 (`--print`): 非交互式输出
   - 安全模式 (`--safe`): 严格权限检查
4. **生命周期管理**:
   - Agent配置文件监控
   - 会话恢复功能
   - 优雅退出处理

**关键导出**:
```typescript
// 主函数
async function main()
async function parseArgs(stdinContent, renderContext): Command

// 配置相关
function completeOnboarding(): void
async function showSetupScreens(safeMode?, print?): Promise<void>
async function setup(cwd, safeMode?): Promise<void>
```

**主要依赖**:
```
FROM:
├── @utils/config (配置管理)
├── @utils/model (模型管理)
├── @screens/REPL (核心UI)
├── @tools (工具系统)
├── @commands (命令系统)
├── @services/mcpClient (MCP服务)
└── @services/sentry (错误追踪)

TO: 无 (作为入口点，被外部调用)
```

**执行流程**:
```
1. main() 启动
   ↓
2. enableConfigs() - 配置验证
   ↓
3. parseArgs() - 解析命令行参数
   ↓
4. showSetupScreens() - 显示设置向导
   ↓
5. setup() - 初始化工作环境
   ↓
6. 根据模式分支:
   - 交互模式: render(<REPL />)
   - 打印模式: ask() + console.log()
```

#### **src/entrypoints/mcp.ts**
**主要功能**:
- MCP (Model Context Protocol) 服务器实现
- 暴露Kode功能给其他MCP客户端

---

### 2.2 核心UI层 (`src/screens/`)

#### **src/screens/REPL.tsx** (809行)
**主要功能**:
- 核心交互式界面
- 消息流管理和渲染
- 用户输入处理
- 工具权限请求UI

**核心状态**:
```typescript
interface REPLState {
  abortController: AbortController | null
  isLoading: boolean
  toolJSX: {jsx: ReactNode, shouldHidePromptInput: boolean} | null
  toolUseConfirm: ToolUseConfirm | null
  messages: MessageType[]
  inputValue: string
  inputMode: 'bash' | 'prompt' | 'koding'
  binaryFeedbackContext: BinaryFeedbackContext | null
}
```

**关键功能函数**:
```typescript
// 初始化查询
async function onInit(): Promise<void>
// 查询AI
async function onQuery(newMessages, passedAbortController?): Promise<void>
// 取消请求
function onCancel(): void
```

**消息渲染策略**:
1. **Static渲染**: 已完成的消息（使用Ink的`<Static>`组件）
2. **Transient渲染**: 进行中的消息（动态更新）
3. 根据`shouldRenderStatically()`判断渲染方式

**主要依赖**:
```
FROM:
├── @query (LLM查询核心)
├── @components/PromptInput (输入组件)
├── @components/Message (消息显示)
├── @components/permissions/PermissionRequest (权限请求)
├── @hooks/useCancelRequest (取消请求hook)
├── @utils/messages (消息处理工具)
└── @context/PermissionContext (权限上下文)

TO:
└── cli.tsx (被渲染调用)
```

**UI组件树**:
```
<REPL>
├── <PermissionProvider>
│   ├── <ModeIndicator />
│   ├── <Static> (已完成消息)
│   │   ├── <Logo />
│   │   ├── <ProjectOnboarding />
│   │   └── <Message> (多个)
│   ├── Transient Messages (进行中)
│   ├── <Spinner /> (加载状态)
│   ├── <BinaryFeedback /> (A/B测试)
│   ├── <PermissionRequest /> (权限请求)
│   ├── <CostThresholdDialog /> (成本提醒)
│   ├── <PromptInput /> (输入框)
│   └── <MessageSelector /> (消息选择器)
```

#### **src/screens/Doctor.tsx**
**主要功能**: 系统健康检查界面

#### **src/screens/LogList.tsx**
**主要功能**: 会话日志列表查看

#### **src/screens/ResumeConversation.tsx**
**主要功能**: 恢复历史对话界面

---

## 3. 配置与模型管理

### 3.1 配置系统 (`src/utils/config.ts`) (939行)

**主要功能**:
- 全局和项目级配置管理
- 模型配置(ModelProfile)管理
- MCP服务器配置
- GPT-5配置自动修复

**核心数据结构**:
```typescript
// 全局配置
type GlobalConfig = {
  projects?: Record<string, ProjectConfig>
  numStartups: number
  theme: ThemeNames
  hasCompletedOnboarding?: boolean
  verbose: boolean
  primaryProvider?: ProviderType

  // 模型系统
  modelProfiles?: ModelProfile[]      // 模型配置列表
  modelPointers?: ModelPointers       // 模型指针
  defaultModelName?: string           // 默认模型

  // MCP配置
  mcpServers?: Record<string, McpServerConfig>

  // OAuth
  oauthAccount?: AccountInfo
}

// 模型配置
type ModelProfile = {
  name: string                        // 友好名称
  provider: ProviderType              // 提供商
  modelName: string                   // 实际模型标识符（主键）
  baseURL?: string                    // 自定义端点
  apiKey: string                      // API密钥
  maxTokens: number                   // 输出token限制
  contextLength: number               // 上下文窗口大小
  reasoningEffort?: 'low' | 'medium' | 'high' // GPT-5推理级别
  isActive: boolean                   // 是否启用
  createdAt: number                   // 创建时间
  lastUsed?: number                   // 最后使用时间

  // GPT-5特定
  isGPT5?: boolean
  validationStatus?: 'valid' | 'needs_repair' | 'auto_repaired'
  lastValidation?: number
}

// 模型指针系统
type ModelPointers = {
  main: string      // 主对话模型
  task: string      // Task工具模型
  reasoning: string // 推理模型
  quick: string     // 快速模型
}

// 项目配置
type ProjectConfig = {
  allowedTools: string[]
  context: Record<string, string>
  dontCrawlDirectory?: boolean
  enableArchitectTool?: boolean
  mcpServers?: Record<string, McpServerConfig>
  approvedMcprcServers?: string[]
  rejectedMcprcServers?: string[]
  hasTrustDialogAccepted?: boolean
}
```

**核心函数**:
```typescript
// 配置读写
export function getGlobalConfig(): GlobalConfig
export function saveGlobalConfig(config: GlobalConfig): void
export function getCurrentProjectConfig(): ProjectConfig
export function saveCurrentProjectConfig(config: ProjectConfig): void

// CLI配置操作
export function getConfigForCLI(key, global): unknown
export function setConfigForCLI(key, value, global): void
export function deleteConfigForCLI(key, global): void
export function listConfigForCLI(global): object

// GPT-5配置管理
export function isGPT5ModelName(modelName: string): boolean
export function validateAndRepairGPT5Profile(profile): ModelProfile
export function validateAndRepairAllGPT5Profiles(): {repaired, total}
export function createGPT5ModelProfile(...): ModelProfile
export function getGPT5ConfigRecommendations(modelName): Partial<ModelProfile>

// 模型指针
export function setModelPointer(pointer, modelName): void
export function setAllPointersToModel(modelName): void
```

**配置文件位置**:
```
全局配置: ~/.kode.json
项目配置: ./.kode.json (合并到全局配置的projects字段)
MCP配置: ./.mcprc
```

**依赖关系**:
```
FROM:
├── @utils/env (环境变量)
├── @utils/state (状态管理)
├── @utils/json (JSON解析)
├── @utils/theme (主题)
└── @utils/debugLogger (调试日志)

TO: 几乎所有模块 (作为基础设施)
```

---

### 3.2 模型管理系统 (`src/utils/model.ts`) (905行)

**主要功能**:
- 统一的模型选择和切换
- 多模型配置管理
- 上下文兼容性检查
- 模型fallback机制

**核心类: ModelManager**
```typescript
export class ModelManager {
  private config: any
  private modelProfiles: ModelProfile[]

  // 模型获取
  getCurrentModel(): string | null          // 终端模型（交互会话）
  getMainAgentModel(): string | null        // 主Agent模型（非终端/MCP）
  getTaskToolModel(): string | null         // Task工具模型
  getReasoningModel(): string | null        // 推理模型
  getQuickModel(): string | null            // 快速模型

  // 模型切换
  switchToNextModel(currentContextTokens): {...}
  switchToNextModelWithContextCheck(contextTokens): {...}
  revertToPreviousModel(previousModelName): boolean

  // 上下文管理
  analyzeContextCompatibility(model, contextTokens): {...}
  canModelHandleContext(model, contextTokens): boolean
  findModelWithSufficientContext(models, contextTokens): ModelProfile | null

  // 模型配置
  addModel(config): Promise<string>
  removeModel(modelName): void
  setPointer(pointer, modelName): void

  // 查询方法
  getModel(pointer): ModelProfile | null
  getModelName(pointer): string | null
  getAvailableModels(): ModelProfile[]
  getAllConfiguredModels(): ModelProfile[]

  // 模型解析（统一查找）
  resolveModel(modelParam): ModelProfile | null
  resolveModelWithInfo(modelParam): {success, profile, error?}
}
```

**模型切换逻辑**:
```
1. 获取所有配置的模型（active + inactive）
2. 按创建时间排序（保证顺序一致性）
3. 找到当前模型位置
4. 切换到下一个模型（循环）
5. 如果目标模型是inactive，自动激活
6. 检查上下文兼容性
7. 更新配置并返回结果
```

**上下文兼容性分析**:
```typescript
analyzeContextCompatibility(model, contextTokens) {
  const usableContext = model.contextLength * 0.8  // 保留20%给输出
  const usagePercentage = (contextTokens / usableContext) * 100

  if (usagePercentage <= 70) return {severity: 'safe', ...}
  if (usagePercentage <= 90) return {severity: 'warning', ...}
  return {severity: 'critical', compatible: false, ...}
}
```

**单例模式**:
```typescript
let globalModelManager: ModelManager | null = null

export const getModelManager = (): ModelManager => {
  if (!globalModelManager) {
    globalModelManager = new ModelManager(getGlobalConfig())
  }
  return globalModelManager
}

export const reloadModelManager = (): void => {
  globalModelManager = null
  getModelManager() // 强制创建新实例
}
```

**依赖关系**:
```
FROM:
├── @utils/config (配置读写)
└── @utils/log (日志记录)

TO:
├── @services/claude (AI服务)
├── @tools/TaskTool (Task工具)
├── @components/ModelSelector (模型选择器)
└── query.ts (查询系统)
```

---

## 4. AI服务集成层

### 4.1 查询系统 (`src/query.ts`) (685行)

**主要功能**:
- LLM查询的核心流程
- 工具调用协调
- 并发/串行工具执行
- 二元反馈（A/B测试）

**核心函数**:
```typescript
export async function* query(
  messages: Message[],
  systemPrompt: string[],
  context: {[k: string]: string},
  canUseTool: CanUseToolFn,
  toolUseContext: ExtendedToolUseContext,
  getBinaryFeedbackResponse?: (m1, m2) => Promise<BinaryFeedbackResult>
): AsyncGenerator<Message, void>
```

**执行流程**:
```
1. query() 开始
   ↓
2. checkAutoCompact() - 自动压缩长对话
   ↓
3. formatSystemPromptWithContext() - 构建系统提示
   ↓
4. queryLLM() - 调用AI服务
   ↓
5. 如果有tool_use消息:
   ├→ canRunConcurrently?
   │  ├─ Yes → runToolsConcurrently()
   │  └─ No  → runToolsSerially()
   ↓
6. 递归调用query() (如果工具执行需要后续对话)
```

**工具执行策略**:
```typescript
// 并发执行（只读工具）
async function* runToolsConcurrently(toolUseMessages, ...) {
  for await (const message of runTools(
    toolUseMessages,
    MAX_TOOL_USE_CONCURRENCY  // 最多10个并发
  )) {
    yield message
  }
}

// 串行执行（写操作工具）
async function* runToolsSerially(toolUseMessages, ...) {
  for (const toolUse of toolUseMessages) {
    for await (const message of runTools([toolUse], 1)) {
      yield message
    }
  }
}
```

**依赖关系**:
```
FROM:
├── @services/claude (queryLLM)
├── @services/systemReminder (提醒系统)
├── @utils/toolExecutionController (工具执行)
├── @utils/messages (消息处理)
└── @utils/autoCompactCore (自动压缩)

TO:
└── @screens/REPL (被REPL调用)
```

---

### 4.2 Claude服务 (`src/services/claude.ts`) (2,165行)

**主要功能**:
- Anthropic Claude API集成
- 流式响应处理
- Token计数和成本追踪
- 错误处理和重试

**核心函数**:
```typescript
// 主查询函数
export async function queryLLM(
  messages: MessageParam[],
  systemPrompt: string[],
  maxThinkingTokens: number,
  tools: Tool[],
  abortSignal: AbortSignal,
  options?: {...}
): Promise<AssistantMessage>

// 系统提示构建
export function formatSystemPromptWithContext(
  systemPrompt: string[],
  context: {[k: string]: string},
  agentId?: string
): {systemPrompt: string[], reminders: string}

// 模型查询（统一接口）
export async function queryModel(
  messages: MessageParam[],
  systemPrompt: string[],
  tools: Tool[],
  abortSignal: AbortSignal,
  options?: {...}
): Promise<APIResponse>
```

**提供商适配**:
```typescript
// 根据模型类型选择适配器
const profile = modelManager.resolveModel(model)

switch (profile.provider) {
  case 'anthropic':
    return createAnthropicAdapter(profile)
  case 'openai':
  case 'custom-openai':
    return createOpenAIAdapter(profile)
  case 'gemini':
    return createGeminiAdapter(profile)
  // ... 更多提供商
}
```

**流式处理**:
```typescript
for await (const chunk of stream) {
  switch (chunk.type) {
    case 'message_start':
      // 初始化消息
    case 'content_block_delta':
      // 累积内容
    case 'message_delta':
      // 更新token统计
    case 'message_stop':
      // 完成消息
  }
}
```

**依赖关系**:
```
FROM:
├── @anthropic-ai/sdk
├── @services/modelAdapterFactory (适配器工厂)
├── @utils/model (模型管理)
├── @utils/messageContextManager (上下文管理)
└── @costTracker (成本追踪)

TO:
└── @query (被查询系统调用)
```

---

### 4.3 OpenAI服务 (`src/services/openai.ts`) (1,361行)

**主要功能**:
- OpenAI API集成
- GPT-5 Responses API支持
- 自定义OpenAI兼容端点支持
- Reasoning effort配置

**特殊功能: GPT-5支持**
```typescript
// GPT-5 Responses API (预览功能)
if (isGPT5Model && supportsResponsesAPI) {
  // 使用新的Responses API
  const response = await openai.chat.completions.create({
    model: modelName,
    messages,
    max_completion_tokens: maxTokens,  // GPT-5使用不同参数
    reasoning_effort: reasoningEffort, // 推理级别
    response_format: {...}
  })

  // 状态管理（conversationId, responseId）
  toolUseContext.responseState = {
    conversationId: response.id,
    previousResponseId: response.id
  }
}
```

**依赖关系**:
```
FROM:
├── openai SDK
├── @utils/model (模型管理)
└── @services/responseStateManager (GPT-5状态)

TO:
└── @services/modelAdapterFactory (被适配器工厂调用)
```

---

### 4.4 模型适配器工厂 (`src/services/modelAdapterFactory.ts`)

**主要功能**:
- 统一不同AI提供商的接口
- 适配器模式实现
- 动态加载适配器

**核心接口**:
```typescript
interface ModelAdapter {
  createMessage(params): Promise<APIResponse>
  streamMessage(params): AsyncIterable<StreamChunk>
  countTokens(messages): Promise<number>
  formatToolsForAPI(tools): any[]
}
```

**支持的提供商**:
- Anthropic (Claude)
- OpenAI (GPT-4, GPT-5)
- Mistral
- DeepSeek
- Gemini
- Kimi (Moonshot)
- Qwen (阿里)
- GLM (智谱)
- MiniMax
- Azure OpenAI
- Custom OpenAI兼容端点

---

## 5. 工具系统

### 5.1 工具基础架构

#### **src/Tool.ts** (86行)
**主要功能**: 定义工具系统的核心接口

**核心接口**:
```typescript
export interface Tool<TInput extends z.ZodObject<any>, TOutput = any> {
  name: string                            // 工具名称
  description?: () => Promise<string>     // 工具描述（异步）
  inputSchema: TInput                     // Zod验证模式
  inputJSONSchema?: Record<string, unknown> // JSON Schema

  // 系统提示
  prompt: (options?: {safeMode?: boolean}) => Promise<string>

  // 工具元信息
  userFacingName?: () => string
  isEnabled: () => Promise<boolean>
  isReadOnly: () => boolean                // 是否只读（影响并发）
  isConcurrencySafe: () => boolean         // 是否并发安全
  needsPermissions: (input?) => boolean    // 是否需要权限

  // 输入验证
  validateInput?: (
    input: z.infer<TInput>,
    context?: ToolUseContext
  ) => Promise<ValidationResult>

  // 渲染方法
  renderResultForAssistant: (output: TOutput) => string | any[]
  renderToolUseMessage: (input, options) => string
  renderToolUseRejectedMessage?: (...args) => ReactElement
  renderToolResultMessage?: (output: TOutput) => ReactElement

  // 核心执行方法
  call: (
    input: z.infer<TInput>,
    context: ToolUseContext
  ) => AsyncGenerator<
    | {type: 'result'; data: TOutput; resultForAssistant?: string}
    | {type: 'progress'; content: any; normalizedMessages?: any[]; tools?: any[]},
    void,
    unknown
  >
}

export interface ToolUseContext {
  messageId: string | undefined
  agentId?: string
  safeMode?: boolean
  abortController: AbortController
  readFileTimestamps: {[filePath: string]: number}
  options?: {
    commands?: any[]
    tools?: any[]
    verbose?: boolean
    slowAndCapableModel?: string
    safeMode?: boolean
    forkNumber?: number
    messageLogName?: string
    maxThinkingTokens?: any
    isKodingRequest?: boolean
    kodingContext?: string
    isCustomCommand?: boolean
  }
  responseState?: {
    previousResponseId?: string
    conversationId?: string
  }
}
```

---

#### **src/tools.ts** (68行)
**主要功能**: 工具注册和导出

**工具列表**:
```typescript
export const getAllTools = (): Tool[] => [
  TaskTool,              // Task代理工具
  AskExpertModelTool,    // 询问专家模型
  BashTool,              // Bash命令执行
  GlobTool,              // 文件模式匹配
  GrepTool,              // 代码搜索
  LSTool,                // 目录列表
  FileReadTool,          // 文件读取
  FileEditTool,          // 文件编辑
  MultiEditTool,         // 多文件编辑
  FileWriteTool,         // 文件写入
  NotebookReadTool,      // Jupyter笔记本读取
  NotebookEditTool,      // Jupyter笔记本编辑
  ThinkTool,             // 思考工具
  TodoWriteTool,         // Todo列表
  WebSearchTool,         // Web搜索
  URLFetcherTool,        // URL获取
  MemoryReadTool,        // 记忆读取（内部）
  MemoryWriteTool,       // 记忆写入（内部）
]
```

**工具过滤**:
```typescript
export const getTools = memoize(
  async (enableArchitect?: boolean): Promise<Tool[]> => {
    const tools = [...getAllTools(), ...(await getMCPTools())]

    // 可选加载Architect工具
    if (enableArchitect) {
      tools.push(ArchitectTool)
    }

    // 过滤禁用的工具
    const isEnabled = await Promise.all(tools.map(tool => tool.isEnabled()))
    return tools.filter((_, i) => isEnabled[i])
  }
)

export const getReadOnlyTools = memoize(async (): Promise<Tool[]> => {
  const tools = getAllTools().filter(tool => tool.isReadOnly())
  const isEnabled = await Promise.all(tools.map(tool => tool.isEnabled()))
  return tools.filter((_, i) => isEnabled[i])
})
```

---

### 5.2 核心工具详解

#### **TaskTool** (`src/tools/TaskTool/TaskTool.tsx`) (485行)
**主要功能**:
- 启动子Agent执行独立任务
- 支持动态Agent加载
- 工具过滤和权限控制

**工作流程**:
```
1. 用户请求TaskTool
   ↓
2. 加载Agent配置（从.kode/agents/或.claude/agents/）
   ↓
3. 过滤工具列表（根据Agent的tools配置）
   ↓
4. 创建新的query()调用（独立上下文）
   ↓
5. 流式返回子Agent的执行结果
   ↓
6. 合并到主对话流
```

**Agent配置格式**:
```markdown
---
name: agent-name
description: "When to use this agent"
tools: ["FileRead", "Bash"]  # 或 "*" 表示所有工具
model: model-name            # 可选，指定模型
---

Agent的系统提示内容...
```

---

#### **FileReadTool** (`src/tools/FileReadTool/FileReadTool.tsx`) (404行)
**主要功能**:
- 读取文件内容
- 支持偏移量和行数限制
- 图片、PDF、Jupyter笔记本支持

**参数**:
```typescript
inputSchema: z.object({
  file_path: z.string(),  // 绝对路径
  offset: z.number().optional(),  // 起始行
  limit: z.number().optional()    // 读取行数
})
```

---

#### **FileEditTool** (`src/tools/FileEditTool/FileEditTool.tsx`) (317行)
**主要功能**:
- 精确字符串替换
- 保留缩进
- replace_all支持

**参数**:
```typescript
inputSchema: z.object({
  file_path: z.string(),
  old_string: z.string(),
  new_string: z.string(),
  replace_all: z.boolean().optional()
})
```

**验证逻辑**:
- 检查文件是否被读取过（readFileTimestamps）
- 确保old_string唯一（除非replace_all=true）
- 行号前缀处理

---

#### **BashTool** (`src/tools/BashTool/BashTool.tsx`)
**主要功能**:
- 执行Shell命令
- 后台任务支持
- 超时控制
- 持久Shell会话（PersistentShell）

**参数**:
```typescript
inputSchema: z.object({
  command: z.string(),
  description: z.string().optional(),
  timeout: z.number().optional(),  // 最大600000ms
  run_in_background: z.boolean().optional()
})
```

---

#### **GrepTool** (`src/tools/GrepTool/GrepTool.tsx`)
**主要功能**:
- 基于ripgrep的代码搜索
- 支持正则表达式
- 文件类型过滤
- 多种输出模式

**参数**:
```typescript
inputSchema: z.object({
  pattern: z.string(),              // 搜索模式
  path: z.string().optional(),      // 搜索路径
  glob: z.string().optional(),      // 文件过滤
  type: z.string().optional(),      // 文件类型
  output_mode: z.enum(['content', 'files_with_matches', 'count']),
  '-i': z.boolean().optional(),     // 忽略大小写
  '-n': z.boolean().optional(),     // 显示行号
  '-A': z.number().optional(),      // 后续行数
  '-B': z.number().optional(),      // 前置行数
  '-C': z.number().optional(),      // 前后行数
  multiline: z.boolean().optional() // 多行模式
})
```

---

#### **GlobTool** (`src/tools/GlobTool/GlobTool.tsx`)
**主要功能**:
- 文件模式匹配
- 支持glob语法 (**, *, ?)

**参数**:
```typescript
inputSchema: z.object({
  pattern: z.string(),  // 如 "**/*.ts"
  path: z.string().optional()
})
```

---

#### **MultiEditTool** (`src/tools/MultiEditTool/MultiEditTool.tsx`) (397行)
**主要功能**:
- 批量文件编辑
- 原子性操作（全部成功或全部回滚）
- 并发编辑支持

---

#### **TodoWriteTool** (`src/tools/TodoWriteTool/TodoWriteTool.tsx`) (313行)
**主要功能**:
- 任务列表管理
- 状态追踪 (pending/in_progress/completed)
- 实时UI更新

**数据结构**:
```typescript
inputSchema: z.object({
  todos: z.array(z.object({
    content: z.string(),        // 任务描述（命令式）
    activeForm: z.string(),     // 进行中形式（现在进行时）
    status: z.enum(['pending', 'in_progress', 'completed'])
  }))
})
```

---

#### **WebSearchTool** (`src/tools/WebSearchTool/WebSearchTool.tsx`)
**主要功能**:
- Web搜索集成
- 域名过滤

---

#### **URLFetcherTool** (`src/tools/URLFetcherTool/URLFetcherTool.tsx`)
**主要功能**:
- 获取网页内容
- HTML转Markdown
- 重定向处理

---

#### **NotebookReadTool / NotebookEditTool**
**主要功能**:
- Jupyter笔记本支持
- Cell级别操作

---

#### **ThinkTool** (`src/tools/ThinkTool/ThinkTool.tsx`)
**主要功能**:
- 内部思考工具
- 不显示给用户

---

#### **AskExpertModelTool** (`src/tools/AskExpertModelTool/AskExpertModelTool.tsx`) (576行)
**主要功能**:
- 咨询专家模型
- 支持自定义系统提示

---

### 5.3 工具权限系统

#### **权限检查流程**:
```
1. Tool被调用
   ↓
2. needsPermissions() 检查是否需要权限
   ↓
3. canUseTool() Hook被调用
   ↓
4. 显示 <PermissionRequest> UI
   ↓
5. 用户响应:
   - Approve → 执行工具
   - Reject → 返回拒绝消息
   - Bypass All → 跳过后续权限检查
```

#### **权限模式**:
```typescript
// 安全模式 (--safe)
- 所有工具都需要权限
- 不能root运行

// 宽松模式 (默认)
- 预批准的工具跳过权限
- 可以使用Bypass All
```

---

## 6. 命令系统

### 6.1 命令架构 (`src/commands.ts` 和 `src/commands/`)

**命令注册**:
```typescript
export interface Command {
  name: string
  description: string
  isHidden?: boolean
  execute: (args?: string) => Promise<string | void>
}

export async function getCommands(): Promise<Command[]> {
  return [
    agentsCommand,
    configCommand,
    modelCommand,
    modelStatusCommand,
    mcpCommand,
    loginCommand,
    logoutCommand,
    onboardingCommand,
    clearCommand,
    compactCommand,
    resumeCommand,
    releaseNotesCommand,
    refreshCommandsCommand,
    // ... 更多命令
  ]
}
```

---

### 6.2 重要命令详解

#### **/agents** (`src/commands/agents.tsx`) (3,416行)
**主要功能**:
- Agent配置管理UI
- 添加/编辑/删除Agent
- Agent优先级管理

**UI组件**:
```
<AgentsCommand>
├── <AgentList> (列表视图)
│   ├── Agent配置卡片
│   └── 操作菜单
└── <AgentEditor> (编辑器)
    ├── 名称输入
    ├── 描述输入
    ├── 工具选择器
    └── 系统提示编辑器
```

---

#### **/model** (`src/commands/model.tsx`)
**主要功能**:
- 模型配置管理
- 添加/删除模型
- 指针分配
- 模型切换

**核心功能**:
```typescript
// 添加模型
async function addModel(profile: ModelProfile)

// 删除模型
async function removeModel(modelName: string)

// 设置指针
async function setPointer(pointer: ModelPointerType, modelName: string)

// 列出模型
async function listModels()
```

---

#### **/config** (`src/commands/config.tsx`)
**主要功能**:
- 配置查看和修改
- 全局/项目配置切换

---

#### **/mcp** (`src/commands/mcp.ts`)
**主要功能**:
- MCP服务器管理
- 添加/删除/列出服务器
- 从Claude Desktop导入

---

#### **/login / /logout** (`src/commands/login.tsx`, `src/commands/logout.tsx`)
**主要功能**:
- OAuth身份验证
- API密钥管理

---

#### **/resume** (`src/commands/resume.ts`)
**主要功能**:
- 恢复历史对话

---

#### **/clear** (`src/commands/clear.ts`)
**主要功能**:
- 清屏

---

#### **/compact** (`src/commands/compact.ts`)
**主要功能**:
- 手动压缩对话上下文

---

## 7. UI组件系统

### 7.1 核心UI组件

#### **ModelSelector** (`src/components/ModelSelector.tsx`) (3,486行)
**主要功能**:
- 多模型配置UI
- 模型切换界面
- 指针分配UI
- 上下文兼容性显示

**UI结构**:
```
<ModelSelector>
├── 模型列表
│   ├── 模型卡片
│   │   ├── 名称 / 提供商
│   │   ├── 上下文长度
│   │   ├── Max Tokens
│   │   └── 状态 (Active/Inactive)
│   └── 操作按钮
├── 模型指针配置
│   ├── Main
│   ├── Task
│   ├── Reasoning
│   └── Quick
└── 添加模型向导
    ├── 提供商选择
    ├── API配置
    └── 参数配置
```

---

#### **PromptInput** (`src/components/PromptInput.tsx`) (736行)
**主要功能**:
- 多模式输入 (prompt / bash / koding)
- 命令自动补全
- Agent提及 (@agent)
- 文件提及
- 历史记录

**输入模式**:
```typescript
type InputMode = 'prompt' | 'bash' | 'koding'

// prompt模式: 普通对话
// bash模式: 直接执行Shell命令
// koding模式: 自动保存到AGENTS.md/CLAUDE.md
```

**自动补全**:
- 斜杠命令: /agents, /model, /config
- Agent提及: @explorer, @code-reviewer
- 文件路径: ./src/**, *.ts
- 系统命令: ls, grep, cd（bash模式）

---

#### **Message** (`src/components/Message.tsx`)
**主要功能**:
- 消息渲染
- 语法高亮
- 工具使用显示

---

#### **MessageResponse** (`src/components/MessageResponse.tsx`)
**主要功能**:
- Assistant响应显示
- 进度指示器

---

### 7.2 消息组件 (`src/components/messages/`)

**消息类型**:
- **AssistantTextMessage**: 普通文本
- **AssistantThinkingMessage**: 思考块
- **AssistantRedactedThinkingMessage**: 隐藏思考
- **AssistantToolUseMessage**: 工具使用
- **AssistantBashOutputMessage**: Bash输出
- **UserPromptMessage**: 用户输入
- **UserTextMessage**: 用户文本
- **UserBashInputMessage**: Bash命令
- **UserKodingInputMessage**: Koding输入
- **UserCommandMessage**: 斜杠命令
- **UserToolResultMessage**: 工具结果
- **TaskToolMessage**: Task工具消息
- **TaskProgressMessage**: Task进度

---

### 7.3 权限组件 (`src/components/permissions/`)

#### **PermissionRequest** (`src/components/permissions/PermissionRequest.tsx`)
**主要功能**:
- 统一权限请求UI
- 动态加载工具特定组件

**组件映射**:
```typescript
const permissionComponents = {
  'FileEdit': FileEditPermissionRequest,
  'FileWrite': FileWritePermissionRequest,
  'Bash': BashPermissionRequest,
  'FileRead': FilesystemPermissionRequest,
  'Glob': FilesystemPermissionRequest,
  'Grep': FilesystemPermissionRequest,
  // 默认: FallbackPermissionRequest
}
```

---

#### **FileEditPermissionRequest** (`src/components/permissions/FileEditPermissionRequest/`)
**主要功能**:
- 显示编辑diff
- 语法高亮
- 批准/拒绝按钮

---

#### **BashPermissionRequest** (`src/components/permissions/BashPermissionRequest/`)
**主要功能**:
- 显示命令内容
- 安全警告
- 批准/拒绝按钮

---

### 7.4 其他组件

#### **Logo** (`src/components/Logo.tsx`)
**主要功能**:
- 显示Kode Logo
- MCP服务器状态
- 更新通知横幅

---

#### **Cost** (`src/components/Cost.tsx`)
**主要功能**:
- 成本追踪显示
- 会话成本统计

---

#### **Config** (`src/components/Config.tsx`)
**主要功能**:
- 配置查看器

---

#### **Spinner** (`src/components/Spinner.tsx`)
**主要功能**:
- 加载动画

---

#### **CustomSelect** (`src/components/CustomSelect/`)
**主要功能**:
- 自定义选择器组件
- 键盘导航
- 主题支持

---

## 8. Hooks系统

### 8.1 核心Hooks

#### **useUnifiedCompletion** (`src/hooks/useUnifiedCompletion.ts`) (1,404行)
**主要功能**:
- 统一的自动补全系统
- 支持命令、Agent、文件路径补全
- 智能上下文感知

**补全类型**:
```typescript
interface UnifiedSuggestion {
  value: string
  displayValue: string
  type: 'command' | 'agent' | 'file' | 'ask'
  icon?: string
  score: number
  metadata?: any
  isSmartMatch?: boolean
  originalContext?: 'mention' | 'file' | 'command'
}
```

**工作流程**:
```
1. 用户输入
   ↓
2. getWordAtCursor() - 检测光标处的单词
   ↓
3. 识别上下文类型:
   - 以 / 开头 → 命令补全
   - 以 @ 开头 → Agent补全
   - 路径模式 → 文件补全
   ↓
4. generateSuggestions() - 生成建议
   ↓
5. 显示补全列表
   ↓
6. Tab键循环 / Enter确认
```

---

#### **useTextInput** (`src/hooks/useTextInput.ts`) (317行)
**主要功能**:
- 底层键盘输入处理
- 光标管理
- 历史记录导航

---

#### **useCancelRequest** (`src/hooks/useCancelRequest.ts`)
**主要功能**:
- Ctrl+C处理
- 请求取消逻辑

---

#### **useCanUseTool** (`src/hooks/useCanUseTool.ts`)
**主要功能**:
- 工具权限检查Hook
- 返回权限请求Promise

---

#### **useLogMessages** (`src/hooks/useLogMessages.ts`)
**主要功能**:
- 消息日志记录
- 自动保存到文件

---

#### **useApiKeyVerification** (`src/hooks/useApiKeyVerification.ts`)
**主要功能**:
- API密钥验证
- 状态管理

---

#### **useLogStartupTime** (`src/hooks/useLogStartupTime.ts`)
**主要功能**:
- 启动时间记录
- 性能监控

---

## 9. 工具类和基础设施

### 9.1 调试和日志

#### **debugLogger** (`src/utils/debugLogger.ts`) (1,232行)
**主要功能**:
- 结构化调试日志
- 请求追踪
- 性能标记
- 多级别日志

**日志类型**:
```typescript
export function debug(category: string, data: Record<string, any>): void
export function info(category: string, data: Record<string, any>): void
export function warn(category: string, data: Record<string, any>): void
export function error(category: string, data: Record<string, any>): void

// 用户友好日志
export function logUserFriendly(
  level: 'info' | 'warn' | 'error',
  message: string
): void

// 性能追踪
export function markPhase(phase: string): void

// 请求追踪
export function getCurrentRequest(): RequestTracker | null
export function startRequest(requestId: string): RequestTracker
```

**日志示例**:
```typescript
debugLogger.info('MODEL_SWITCH', {
  from: 'gpt-4',
  to: 'claude-sonnet-4',
  reason: 'user_request'
})

markPhase('QUERY_INIT')
markPhase('LLM_CALL')
markPhase('TOOL_EXECUTION')
```

---

#### **log** (`src/utils/log.ts`) (420行)
**主要功能**:
- 文件日志记录
- 会话日志管理
- 日志文件命名

**日志路径**:
```
消息日志: ~/.kode/logs/messages/
错误日志: ~/.kode/logs/errors/
```

---

### 9.2 消息处理

#### **messages** (`src/utils/messages.tsx`) (944行)
**主要功能**:
- 消息标准化
- 消息排序
- 用户输入处理
- 消息验证

**核心函数**:
```typescript
// 消息标准化
export function normalizeMessages(messages: Message[]): NormalizedMessage[]
export function normalizeMessagesForAPI(messages: Message[]): MessageParam[]

// 消息排序（工具使用和结果配对）
export function reorderMessages(messages: NormalizedMessage[]): NormalizedMessage[]

// 用户输入处理
export async function processUserInput(
  input: string,
  mode: 'prompt' | 'bash' | 'koding',
  setToolJSX: SetToolJSXFn,
  context: ToolUseContext,
  customCommand: string | null
): Promise<Message[]>

// 工具使用ID管理
export function getUnresolvedToolUseIDs(messages): Set<string>
export function getInProgressToolUseIDs(messages): Set<string>
export function getErroredToolUseMessages(messages): NormalizedMessage[]

// 消息创建
export function createUserMessage(content, options?): UserMessage
export function createAssistantMessage(content, cost?, duration?): AssistantMessage
export function createProgressMessage(...): ProgressMessage
```

**消息流转换**:
```
原始Message[]
  ↓ normalizeMessages()
NormalizedMessage[]
  ↓ reorderMessages()
排序后的NormalizedMessage[]
  ↓ normalizeMessagesForAPI()
API格式MessageParam[]
```

---

### 9.3 文件和系统

#### **file** (`src/utils/file.ts`) (405行)
**主要功能**:
- 文件操作工具
- 路径解析
- 文件系统检查

---

#### **PersistentShell** (`src/utils/PersistentShell.ts`) (550行)
**主要功能**:
- 持久Shell会话
- 工作目录管理
- 环境变量管理

**单例模式**:
```typescript
class PersistentShell {
  private static instance: PersistentShell | null = null

  static getInstance(): PersistentShell {
    if (!this.instance) {
      this.instance = new PersistentShell()
    }
    return this.instance
  }

  async executeCommand(command: string): Promise<string>
  async setCwd(dir: string): Promise<void>
  getCwd(): string
  close(): void
}
```

---

#### **secureFile** (`src/utils/secureFile.ts`) (564行)
**主要功能**:
- 安全文件操作
- 路径验证
- 权限检查

---

### 9.4 其他工具类

#### **Cursor** (`src/utils/Cursor.ts`) (438行)
**主要功能**:
- 光标位置管理

---

#### **todoStorage** (`src/utils/todoStorage.ts`) (431行)
**主要功能**:
- Todo数据持久化

---

#### **fuzzyMatcher** (`src/utils/fuzzyMatcher.ts`) (327行)
**主要功能**:
- 模糊匹配算法
- 用于命令补全

---

#### **agentLoader** (`src/utils/agentLoader.ts`)
**主要功能**:
- Agent配置加载
- 5级优先级系统
- 热重载支持

**优先级**:
```
1. Built-in (代码内置)
2. ~/.claude/agents/ (Claude Code兼容)
3. ~/.kode/agents/ (Kode用户级)
4. ./.claude/agents/ (Claude Code项目级)
5. ./.kode/agents/ (Kode项目级)
```

---

#### **messageContextManager** (`src/utils/messageContextManager.ts`)
**主要功能**:
- 上下文窗口管理
- 智能截断

---

#### **autoCompactCore** (`src/utils/autoCompactCore.ts`)
**主要功能**:
- 自动对话压缩
- Token计数

---

## 10. 类型和常量

### 10.1 类型定义 (`src/types/`)

#### **common.d.ts**
```typescript
export type UUID = string
```

---

#### **conversation.ts**
```typescript
export type Message = UserMessage | AssistantMessage | ProgressMessage
export type UserMessage = {...}
export type AssistantMessage = {...}
export type ProgressMessage = {...}
```

---

#### **modelCapabilities.ts**
```typescript
export type ModelCapabilities = {
  supportsImages: boolean
  supportsThinking: boolean
  supportsPDFUpload: boolean
  supportsWebSearch: boolean
  maxTokens: number
  contextLength: number
}
```

---

#### **PermissionMode.ts**
```typescript
export type PermissionMode = 'safe' | 'permissive'
```

---

#### **RequestContext.ts**
```typescript
export type RequestContext = {
  requestId: string
  timestamp: number
  model: string
  agentId?: string
}
```

---

### 10.2 常量定义 (`src/constants/`)

#### **models.ts** (1,030行)
**主要功能**:
- 所有支持的模型定义
- 模型能力配置
- 默认参数

**模型列表**:
```typescript
export const MODELS = {
  // Anthropic
  'claude-sonnet-4-20250514': {...},
  'claude-3-7-sonnet-20250219': {...},
  'claude-3-5-sonnet-20241022': {...},

  // OpenAI
  'gpt-5-nano': {...},
  'gpt-5-mini': {...},
  'gpt-5': {...},
  'gpt-4o': {...},
  'gpt-4o-mini': {...},
  'o1': {...},
  'o3-mini': {...},

  // DeepSeek
  'deepseek-chat': {...},
  'deepseek-reasoner': {...},

  // Gemini
  'gemini-2.0-flash-exp': {...},
  'gemini-exp-1206': {...},

  // 其他...
}
```

---

#### **modelCapabilities.ts**
**主要功能**:
- 模型能力查询

```typescript
export function getModelCapabilities(modelName: string): ModelCapabilities
```

---

#### **prompts.ts**
**主要功能**:
- 系统提示模板

```typescript
export async function getSystemPrompt(): Promise<string[]>
```

---

#### **macros.ts**
**主要功能**:
- 全局宏定义

```typescript
export const MACRO = {
  VERSION: '0.x.x',
  PRODUCT_NAME: 'Kode',
  // ...
}
```

---

#### **product.ts**
**主要功能**:
- 产品信息

```typescript
export const PRODUCT_NAME = 'Kode'
export const PRODUCT_COMMAND = 'kode'
```

---

## 11. 模块依赖关系图

### 11.1 核心流程依赖

```
cli.tsx (入口)
  ↓
  ├→ config.ts (配置加载)
  │   └→ model.ts (模型管理)
  ↓
  ├→ REPL.tsx (UI渲染)
  │   ↓
  │   ├→ PromptInput.tsx (输入)
  │   │   └→ useUnifiedCompletion.ts (补全)
  │   ↓
  │   ├→ query.ts (查询)
  │   │   ↓
  │   │   ├→ claude.ts / openai.ts (AI服务)
  │   │   │   └→ modelAdapterFactory.ts (适配器)
  │   │   ↓
  │   │   └→ toolExecutionController.ts (工具执行)
  │   │       └→ Tool.call() (工具调用)
  │   ↓
  │   └→ Message.tsx (消息显示)
  ↓
  └→ commands/ (命令处理)
```

---

### 11.2 工具系统依赖

```
tools.ts (工具注册)
  ↓
  ├→ TaskTool
  │   └→ agentLoader.ts (Agent加载)
  ├→ FileReadTool
  │   └→ file.ts (文件工具)
  ├→ FileEditTool
  │   └→ file.ts
  ├→ BashTool
  │   └→ PersistentShell.ts
  ├→ GrepTool
  │   └→ ripgrep (外部命令)
  └→ ... (其他工具)
```

---

### 11.3 配置系统依赖

```
config.ts (配置管理)
  ↓
  ├→ GlobalConfig
  │   ├→ modelProfiles[]
  │   ├→ modelPointers
  │   └→ mcpServers
  ↓
  ├→ ProjectConfig
  │   ├→ allowedTools[]
  │   ├→ mcpServers
  │   └→ context
  ↓
  └→ model.ts (ModelManager)
      └→ 模型切换逻辑
```

---

### 11.4 服务层依赖

```
query.ts (查询入口)
  ↓
  └→ queryLLM()
      ↓
      ├→ ModelManager.resolveModel()
      ↓
      ├→ modelAdapterFactory.createAdapter()
      │   ↓
      │   ├→ AnthropicAdapter
      │   │   └→ @anthropic-ai/sdk
      │   ├→ OpenAIAdapter
      │   │   └→ openai SDK
      │   └→ ... (其他适配器)
      ↓
      └→ Stream Processing
          └→ message accumulation
```

---

### 11.5 UI组件依赖

```
REPL.tsx
  ↓
  ├→ Logo.tsx
  ├→ ProjectOnboarding.tsx
  ├→ ModeIndicator.tsx
  ├→ PromptInput.tsx
  │   ↓
  │   ├→ useTextInput.ts
  │   ├→ useUnifiedCompletion.ts
  │   ├→ useArrowKeyHistory.ts
  │   └→ ModelSelector.tsx (Ctrl+Space)
  ├→ Message.tsx
  │   ↓
  │   ├→ AssistantTextMessage.tsx
  │   ├→ AssistantToolUseMessage.tsx
  │   ├→ UserPromptMessage.tsx
  │   └→ ... (其他消息类型)
  ├→ PermissionRequest.tsx
  │   ↓
  │   ├→ FileEditPermissionRequest.tsx
  │   ├→ BashPermissionRequest.tsx
  │   └→ FallbackPermissionRequest.tsx
  └→ MessageSelector.tsx
```

---

## 12. 关键设计模式

### 12.1 适配器模式
- **用途**: 统一不同AI提供商的API
- **实现**: `modelAdapterFactory.ts`
- **优势**: 易于添加新的AI提供商

### 12.2 工厂模式
- **用途**: 创建模型适配器
- **实现**: `createAdapter(provider)`
- **优势**: 解耦模型选择和使用

### 12.3 单例模式
- **用途**:
  - `ModelManager` (全局实例)
  - `PersistentShell` (Shell会话)
- **优势**: 避免重复初始化，保持状态一致

### 12.4 发布订阅模式
- **用途**: Agent配置热重载
- **实现**: `agentLoader.ts` + 文件监控
- **优势**: 无需重启即可更新配置

### 12.5 策略模式
- **用途**: 工具并发/串行执行
- **实现**: `runToolsConcurrently()` vs `runToolsSerially()`
- **优势**: 根据工具类型选择执行策略

### 12.6 生成器模式
- **用途**: 流式响应处理
- **实现**: `AsyncGenerator` 在 `query()` 和 `Tool.call()`
- **优势**: 实时显示AI响应和工具进度

---

## 13. 数据流图

### 13.1 查询流程

```
用户输入
  ↓
processUserInput()
  ↓
REPL.onQuery()
  ↓
query()
  ├→ formatSystemPromptWithContext()
  ├→ normalizeMessagesForAPI()
  └→ queryLLM()
      ├→ ModelManager.resolveModel()
      ├→ createAdapter()
      └→ adapter.createMessage()
          ↓
      [AI响应流]
          ↓
      yield AssistantMessage
          ↓
      [如果有tool_use]
          ├→ runToolsConcurrently() / runToolsSerially()
          │   ├→ canUseTool() (权限检查)
          │   ├→ Tool.call()
          │   └→ yield ProgressMessage
          ↓
      yield UserMessage (tool_result)
          ↓
      递归调用 query()
          ↓
      [直到无tool_use]
          ↓
      完成
```

---

### 13.2 配置流程

```
启动时:
  enableConfigs()
    ↓
  getGlobalConfig()
    ├→ 读取 ~/.kode.json
    ├→ migrateModelProfilesRemoveId() (迁移旧配置)
    └→ validateAndRepairAllGPT5Profiles() (GPT-5修复)
        ↓
  getCurrentProjectConfig()
    └→ 合并 ./.kode.json

配置修改:
  setConfigForCLI() / saveGlobalConfig()
    ↓
  writeFileSync(~/.kode.json)
    ↓
  reloadModelManager() (如果是模型配置)
```

---

### 13.3 模型切换流程

```
用户按 Tab 或 Ctrl+Tab
  ↓
ModelManager.switchToNextModel(currentContextTokens)
  ↓
  ├→ getAllConfiguredModels() (包括inactive)
  ├→ 按 createdAt 排序
  ├→ 找到当前模型索引
  ├→ 计算下一个索引: (current + 1) % total
  ├→ 激活模型 (如果需要)
  ├→ analyzeContextCompatibility()
  │   └→ 检查上下文是否超标
  ├→ setPointer('main', nextModel)
  └→ updateLastUsed(nextModel)
      ↓
  saveGlobalConfig()
      ↓
  reloadModelManager()
      ↓
  返回切换结果 {success, modelName, contextOverflow, ...}
```

---

## 14. 性能优化要点

### 14.1 Memoization
- `getTools()` - 避免重复加载工具
- `getMcprcConfig()` - 缓存MCP配置
- `getSlowAndCapableModel()` - 缓存模型选择

### 14.2 异步并发
- 工具并发执行 (`MAX_TOOL_USE_CONCURRENCY = 10`)
- 配置并行加载 (`Promise.all`)
- Agent并行扫描

### 14.3 静态渲染
- Ink `<Static>` 组件用于已完成消息
- 减少重渲染次数

### 14.4 懒加载
- Agent配置按需加载
- MCP工具动态加载

### 14.5 上下文管理
- 自动压缩长对话 (`checkAutoCompact`)
- 智能截断消息历史
- 上下文窗口溢出检测

---

## 15. 安全考虑

### 15.1 权限系统
- 所有写操作需要用户批准
- `--safe` 模式严格权限检查
- 禁止root运行（safe模式）

### 15.2 路径验证
- 绝对路径要求
- 沙箱检查（避免访问敏感目录）
- 符号链接检测

### 15.3 命令执行
- Bash命令需要权限
- 超时保护（默认2分钟，最大10分钟）
- 后台任务监控

### 15.4 API密钥管理
- 密钥截断存储（最后20字符）
- 环境变量优先
- OAuth支持

---

## 16. 扩展点

### 16.1 添加新工具
```typescript
// 1. 创建工具文件
// src/tools/MyTool/MyTool.tsx

export const MyTool: Tool = {
  name: 'my_tool',
  description: async () => 'Tool description',
  inputSchema: z.object({...}),
  prompt: async (options) => 'System prompt',
  isEnabled: async () => true,
  isReadOnly: () => false,
  isConcurrencySafe: () => false,
  needsPermissions: () => true,
  renderResultForAssistant: (output) => output,
  renderToolUseMessage: (input, options) => `Using ${input}`,
  async *call(input, context) {
    // 实现逻辑
    yield {type: 'result', data: {...}}
  }
}

// 2. 注册工具
// src/tools.ts
import { MyTool } from './tools/MyTool/MyTool'

export const getAllTools = () => [
  ...,
  MyTool,
]
```

---

### 16.2 添加新AI提供商
```typescript
// 1. 创建适配器
// src/services/adapters/myProviderAdapter.ts

export function createMyProviderAdapter(profile: ModelProfile): ModelAdapter {
  return {
    async createMessage(params) {...},
    async *streamMessage(params) {...},
    async countTokens(messages) {...},
    formatToolsForAPI(tools) {...}
  }
}

// 2. 注册到工厂
// src/services/modelAdapterFactory.ts

export function createAdapter(profile: ModelProfile): ModelAdapter {
  switch (profile.provider) {
    // ...
    case 'my-provider':
      return createMyProviderAdapter(profile)
    // ...
  }
}

// 3. 添加类型
// src/utils/config.ts

export type ProviderType =
  | ...
  | 'my-provider'
```

---

### 16.3 添加新命令
```typescript
// 1. 创建命令文件
// src/commands/myCommand.ts

export const myCommand: Command = {
  name: 'mycommand',
  description: 'Description',
  execute: async (args?: string) => {
    // 实现逻辑
    return 'Result message'
  }
}

// 2. 注册命令
// src/commands.ts

import { myCommand } from './commands/myCommand'

export async function getCommands(): Promise<Command[]> {
  return [
    ...,
    myCommand,
  ]
}
```

---

### 16.4 添加新Agent
```markdown
<!-- ~/.kode/agents/my-agent.md -->
---
name: my-agent
description: "When to use this agent"
tools: ["FileRead", "Grep"]
model: claude-sonnet-4
---

You are an expert at...

Your tasks:
1. ...
2. ...
```

Agent会自动被 `agentLoader.ts` 发现和加载。

---

## 17. 调试和故障排查

### 17.1 启用调试模式
```bash
# 调试输出
kode --debug

# 详细终端输出
kode --debug-verbose

# Verbose模式
kode --verbose
```

---

### 17.2 查看日志
```bash
# 消息日志
cat ~/.kode/logs/messages/YYYY-MM-DD_HH-MM-SS-fff.jsonl

# 错误日志
cat ~/.kode/logs/errors/YYYY-MM-DD_HH-MM-SS-fff.log
```

---

### 17.3 常见问题

**问题**: 模型切换不生效
**原因**: ModelManager缓存未刷新
**解决**: 调用 `reloadModelManager()`

**问题**: Agent不显示
**原因**: Agent配置格式错误
**解决**: 检查YAML frontmatter格式

**问题**: GPT-5配置错误
**原因**: reasoningEffort参数缺失
**解决**: 运行 `/model` 重新配置，或启动时自动修复

**问题**: 工具并发死锁
**原因**: 非只读工具被并发执行
**解决**: 确保 `isReadOnly()` 正确实现

---

## 18. 总结

### 18.1 项目优势
- **模块化设计**: 清晰的模块边界，易于维护
- **多模型支持**: 统一接口支持多种AI提供商
- **可扩展性**: Agent系统、工具系统、命令系统都易于扩展
- **用户体验**: 丰富的CLI UI，实时反馈
- **安全性**: 完善的权限系统和沙箱机制

### 18.2 代码质量
- **TypeScript严格模式**: 类型安全
- **Zod验证**: 运行时类型检查
- **错误处理**: 完善的异常捕获和恢复
- **日志系统**: 结构化调试日志

### 18.3 架构特点
- **三层架构**: 入口层、UI层、业务层
- **适配器模式**: 统一AI服务接口
- **生成器模式**: 流式数据处理
- **单例模式**: 全局状态管理

---

## 附录 A: 关键文件速查

### A.1 必读文件（理解核心）
1. `src/entrypoints/cli.tsx` - 入口点
2. `src/screens/REPL.tsx` - 核心UI
3. `src/query.ts` - 查询流程
4. `src/Tool.ts` - 工具接口
5. `src/utils/config.ts` - 配置系统
6. `src/utils/model.ts` - 模型管理
7. `src/services/claude.ts` - AI服务

### A.2 扩展开发文件
1. `src/tools.ts` - 添加工具
2. `src/commands.ts` - 添加命令
3. `src/services/modelAdapterFactory.ts` - 添加AI提供商
4. `src/utils/agentLoader.ts` - Agent系统

### A.3 UI开发文件
1. `src/components/PromptInput.tsx` - 输入组件
2. `src/components/ModelSelector.tsx` - 模型选择器
3. `src/components/permissions/` - 权限UI
4. `src/hooks/useUnifiedCompletion.ts` - 自动补全

---

## 附录 B: 命令速查

### B.1 斜杠命令
```
/agents      - Agent管理
/model       - 模型配置
/config      - 配置管理
/mcp         - MCP服务器管理
/login       - OAuth登录
/logout      - 登出
/clear       - 清屏
/compact     - 压缩对话
/resume      - 恢复会话
```

### B.2 CLI参数
```bash
kode [prompt]           # 启动交互式会话
kode -p "query"         # 打印模式
kode --safe             # 安全模式
kode --verbose          # 详细输出
kode --debug            # 调试模式
kode --cwd /path        # 指定工作目录

kode config get <key>   # 获取配置
kode config set <key> <value>  # 设置配置
kode mcp add <name> <cmd> [args...]  # 添加MCP服务器
kode resume [N]         # 恢复会话
```

---

## 附录 C: 环境变量

```bash
# AI Provider Keys
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
DEEPSEEK_API_KEY=...
GOOGLE_API_KEY=...

# Bedrock/Vertex
CLAUDE_CODE_USE_BEDROCK=true
CLAUDE_CODE_USE_VERTEX=true
CLOUD_ML_REGION=us-central1

# Debugging
NODE_ENV=development
KODE_DEBUG=true

# Custom Endpoints
CUSTOM_BASE_URL=https://api.example.com/v1
```

---

**文档版本**: 1.0
**生成日期**: 2025-10-25
**作者**: Claude AI (Kode Assistant)
